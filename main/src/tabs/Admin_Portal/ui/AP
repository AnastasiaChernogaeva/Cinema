<template>
    <div class="container">
        <h1 class="card-title">
           {{subtitle}}
        </h1>
        <div class="card">
        <slot/>
         </div>
     </div>
 </template>

<script> 
 import { computed } from "vue";

 export default {
     props:{
         subtitle:{
             type:String,
             required:true,
         }
     },

     setup(props){
         console.log(props.subtitle);
         document.title = `${props.subtitle} | Админский портал`
     }
 }
</script>
